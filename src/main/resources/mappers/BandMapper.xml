<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.BandMapper">
    <insert id="insertOne" parameterType="Band">
        insert into band(no, createMaster, bandName, createAt, category, memberCnt, status)
        values(#{no}, #{createMaster}, #{bandName}, #{createAt}, #{category}, #{memberCnt}, #{status});
    </insert>

    <update id="bandMemberCnt" parameterType="int">
        update band set memberCnt = memberCnt + 1 where no = #{bandNo}
    </update>
</mapper>