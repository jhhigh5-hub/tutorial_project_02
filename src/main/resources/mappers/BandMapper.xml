<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.BandMapper">
    <insert id="insertOne" parameterType="Band" useGeneratedKeys="true" keyProperty="no">
        insert into band(createMaster, bandName, category, memberCnt)
        values(#{createMaster}, #{bandName}, #{category}, #{memberCnt});
    </insert>

    <select id="selectByNo" parameterType="int" resultType="band">
        select * from band where no = #{no}
    </select>

    <select id="selectKeywordByOffset" resultType="Band" parameterType="map">
        select * from band
        where title like #{keyword} or content like #{keyword}
        order by wroteAt desc limit 10 offset #{offset}
    </select>
</mapper>