<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.MemberMapper">
    <insert id="insertOne" parameterType="com.example.tutorial.vo.Member" useGeneratedKeys="true" keyProperty="no">
        insert into member(id, pw, email, name, nickname) values(#{id}, #{pw}, #{email}, #{name}, #{nickname})
    </insert>
    <select id="selectById" resultType="Member" parameterType="String">
        select * from member where id = #{id}
    </select>
    <select id="selectByNickname" resultType="Member" parameterType="String">
        select * from member where nickname = #{nickname}
    </select>

</mapper>