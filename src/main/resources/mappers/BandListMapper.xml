<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.BandListMapper">
    <select id="selectBandList" parameterType="band" resultType="band">
        select * from member join bandJoinRequest on member.id= bandJoinRequest.id join band on
        bandJoinRequest.bandNo=band.no where member.id= #{id}and bandJoinRequest.joinStatus='approved';
    </select>

</mapper>